// 1. Same Inventory
let inventory = ["Laptop", "Mouse", "Keyboard", "Monitor", "Printer"];

// 2. The Loop
// "i" stands for Index
for (let i = 0; i < inventory.length; i++) {
    // This code runs 5 times automatically
    console.log(`Item number ${i}: ${inventory[i]}`);
}